import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smartlib.app',
  appName: 'SmartLib',
  webDir: 'src/web',
  bundledWebRuntime: false
};

export default config;